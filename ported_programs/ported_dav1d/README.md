# Steps to compile DAV1D

If you haven't already, make sure that you have recusively cloned the repo to get the dav1d submodule.

To compile run:
`./setup.sh`

This will generate the libdav1d.a file in the build directory that is then used in dav1d_pico_interface.